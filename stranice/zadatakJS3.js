var firstName = "John";
var lastName = "Doe";

var fullName = `full name = ${firstName} ${lastName}`; //fullName = firstName + " " + lastName;

console.log(fullName);

var x = z === 2? y : 5; //ternarni zapis

//raspisan 

if (z === 2) {
    x = y;
} else {
    x = 5;
}
